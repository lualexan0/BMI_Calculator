#include <unistd.h>

int main (void)
{
    float peso;
        printf("qual o seu peso\n")
    scanf("%f", &peso);

    if (n >= 18.4)
    {
       printf("abaixo do peso\n") 
    }
    if else (peso <= 18.5 && peso == 24)
    {
    printf ("está com peso normal\n")    
    }
    else (peso <= 25 && peso <= 29.9)
    {
    print("está acima do peso\n")   
    }
    
    return(0)
}
